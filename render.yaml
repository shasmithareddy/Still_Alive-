services:
  - type: web
    name: stillalive-backend
    env: node
    region: us-east
    plan: free
    buildCommand: bash build.sh
    startCommand: node backend/server.js
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
